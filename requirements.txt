pysc2
numpy
absl-py
torch  #==2.4.1 # for python 3.8 compatibility using older versions for cuda 11.8 
torchvision  #==0.19.1 # for python 3.8 compatibility using older versions for cuda 11.8 
torchaudio  #==2.4.1 # for python 3.8 compatibility using older versions for cuda 11.8 

# for campatiblity with pysc2 use specific version of protobuff
protobuf==3.20.1

matplotlib
pandas